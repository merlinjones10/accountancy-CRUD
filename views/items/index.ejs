<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Accounting</title>
  </head>
  <body>
    <div class="container">
      <h1 class="mb-4">Accounting</h1>
      <h3 class="mb-4">Income: <%= totals.income %></h3>
      <h3 class="mb-4">Expenses: <%= totals.expenses %></h3>
      <h3 class="mb-4">Profit: <%= totals.profit %></h3>
      <a href="/items/new" class="btn btn-success d-block">New item</a>

      <% items.forEach(item => { %>
      <div class="card mt-4 hover-shadow">
        <div class="card-body">
          <h4 class="card-title"><%= item.title %></h4>
          <div class="card-subtitle text-muted mb-2">
            <%= item.createdAt.toLocaleDateString() %>
          </div>
          <div class="card-text"><%= item.type %></div>
          <div class="card-text mb-2">
            <%= new Intl.NumberFormat('en-GB', { style: 'currency', currency: 'GBP'
            }).format(item.number) %>
          </div>
          <!-- <div class="card-text mb-2"><%= item.description %></div> -->
          <a href="items/<%= item.slug %>" class="btn btn-primary">More info</a>
          <a href="items/edit/<%= item.id %>" class="btn btn-info">Edit</a>
          <form action="/items/<%= item.id %>?_method=DELETE" method="POST" class="d-inline">
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
      </div>
      <% }) %>
    </div>
  </body>
</html>
